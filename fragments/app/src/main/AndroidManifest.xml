<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bartug.fragments">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        >
        <activity android:name=".MainActivity" android:screenOrientation="fullSensor"> //sadece ilk activty yatay olsun görsellik açısından.
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Main2Activity" android:screenOrientation="portrait" /> //2. activity sadece dikey olsun.
        <activity android:name=".Main3Activity" android:screenOrientation="portrait"></activity> 3. activity sadece yatay olsun. Resim ve yazı yatayda hoş gözükmüyor ayarlasakta.
    </application>

</manifest>